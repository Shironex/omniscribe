{
  "name": "omniscribe",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "electron",
      "esbuild",
      "node-pty"
    ]
  },
  "scripts": {
    "dev": "concurrently -k -n web,desktop -c cyan,yellow \"pnpm dev:web\" \"wait-on http://localhost:5173 && pnpm --filter @omniscribe/shared build && pnpm --filter @omniscribe/mcp-server build && pnpm --filter @omniscribe/desktop dev\"",
    "dev:web": "pnpm --filter @omniscribe/web dev",
    "build": "pnpm -r build",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "concurrently": "^9.2.1",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.0",
    "typescript": "^5.5.0",
    "wait-on": "^9.0.3"
  }
}
